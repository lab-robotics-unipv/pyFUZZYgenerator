#ifndef DEFCOMMON_H_{{ model.name }}
#define DEFCOMMON_H_{{ model.name }}

#include <stdint.h>

#define MIN(x, y) (x < y ? x : y)
#define MAX(x, y) (x > y ? x : y)

typedef float dataType;
typedef int8_t int_t;
typedef uint8_t uint_t;

#endif
